import React from 'react'

const ProjectDetails = ({
  suburb,
  suburbImage,
  logo,
  fullAddress,
  content,
}) => {
  return (
    <section>
      {content}
    </section>
  )
}

export default ProjectDetails
